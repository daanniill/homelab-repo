# Infrastructure

Use this directory for reusable configuration, scripts, and infrastructure definitions.

Keep secrets and environment-specific values out of Git. Add example configuration files only when they are useful and safe to publish.
